apiVersion: v1
kind: Service
metadata:
  name: onlyoffice-people-server
spec:
  ports:
  - port: 5050
    name: http
    targetPort: 5050
  selector:
    app: onlyoffice-people-server
  type: ClusterIP
