apiVersion: v1
kind: Service
metadata:
  name: onlyoffice-urlshortener
  labels:
    app: onlyoffice-urlshortener
spec:
  ports:
  - port: 5050
    name: http
    targetPort: 5050
  - port: 9999
    name: urls
    targetPort: 9999
  selector:
    statefulset.kubernetes.io/pod-name: onlyoffice-urlshortener-0
  type: ClusterIP
