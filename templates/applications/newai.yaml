{{- if .Values.newai.enabled }}
apiVersion: apps/v1
kind: {{ .Values.newai.kind }}
metadata:
  name: newai
  namespace: {{ include "docspace.namespace" . | quote }}
  labels:
    app: newai
    {{- if .Values.commonLabels }}
    {{- include "docspace.labels.commonLabels" . | trim | nindent 4 }}
    {{- end }}
  {{- if or .Values.commonAnnotations .Values.newai.annotations }}
  {{- $dictValue := default .Values.commonAnnotations .Values.newai.annotations }}
  annotations: {{- include "docspace.annotations" ( dict "keyName" $dictValue "context" $ ) | nindent 4 }}
  {{- end }}
spec:
  {{- if not .Values.newai.autoscaling.enabled }}
  replicas: {{ default .Values.replicas .Values.newai.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      app: newai
      {{- if .Values.commonLabels }}
      {{- include "docspace.labels.commonLabels" . | trim | nindent 6 }}
      {{- end }}
  {{- if eq .Values.newai.kind "StatefulSet" }}
  serviceName: newai
  {{- end }}
  {{- if .Values.newai.updateStrategy }}
  {{- if eq .Values.newai.kind "Deployment" }}
  strategy: {{- include "docspace.update.strategyType" .Values.newai.updateStrategy }}
  {{- else }}
  updateStrategy: {{- include "docspace.update.strategyType" .Values.newai.updateStrategy }}
  {{- end }}
  {{- end }}
  {{- if eq .Values.newai.kind "StatefulSet" }}
  podManagementPolicy: {{ .Values.newai.podManagementPolicy }}
  {{- end }}
  template:
    metadata:
      labels:
        app: newai
        {{- if .Values.commonLabels }}
        {{- include "docspace.labels.commonLabels" . | trim | nindent 8 }}
        {{- end }}
      {{- if or .Values.podAnnotations .Values.newai.podAnnotations }}
      {{- $dictValue := default .Values.podAnnotations .Values.newai.podAnnotations }}
      annotations: {{- include "docspace.annotations" ( dict "keyName" $dictValue "context" $ ) | nindent 8 }}
      {{- end }}
    spec:
      serviceAccountName: {{ include "docspace.serviceAccountName" . }}
      {{- if or .Values.podSecurityContext.enabled .Values.newai.podSecurityContext.enabled }}
      {{- $psc := dict }}
      {{- if .Values.newai.podSecurityContext.enabled }}
      {{- $psc = .Values.newai.podSecurityContext }}
      {{- else }}
      {{- $psc = .Values.podSecurityContext }}
      {{- end }}
      securityContext: {{- include "docspace.securityContext" $psc | nindent 8 }}
      {{- end }}
      affinity:
        podAntiAffinity:
          {{- if eq .Values.podAntiAffinity.type "preferred" }}
          preferredDuringSchedulingIgnoredDuringExecution:
          - podAffinityTerm:
              labelSelector:
                matchExpressions:
                - key: app
                  operator: In
                  values:
                  - newai
              topologyKey: {{ .Values.podAntiAffinity.topologyKey }}
            weight: {{ .Values.podAntiAffinity.weight }}
          {{- else if eq .Values.podAntiAffinity.type "required" }}
          requiredDuringSchedulingIgnoredDuringExecution:
          - labelSelector:
              matchExpressions:
              - key: app
                operator: In
                values:
                - newai
            topologyKey: {{ .Values.podAntiAffinity.topologyKey }}
          {{- end }}
          {{- with .Values.newai.customPodAntiAffinity }}
          {{- toYaml . | nindent 10 }}
          {{- end }}
        {{- with .Values.newai.podAffinity }}
        podAffinity:
          {{- toYaml . | nindent 10 }}
        {{- end }}
        {{- with .Values.newai.nodeAffinity }}
        nodeAffinity:
          {{- toYaml . | nindent 10 }}
        {{- end }}
      {{- if or .Values.nodeSelector .Values.newai.nodeSelector }}
      nodeSelector: {{ toYaml (default .Values.nodeSelector .Values.newai.nodeSelector) | nindent 8 }}
      {{- end }}
      {{- if or .Values.tolerations .Values.newai.tolerations }}
      tolerations: {{ toYaml (default .Values.tolerations .Values.newai.tolerations) | nindent 8 }}
      {{- end }}
      {{- if .Values.imagePullSecrets }}
      imagePullSecrets:
      - name: {{ .Values.imagePullSecrets }}
      {{- end }}
      {{- if .Values.connections.hostAliases }}
      hostAliases: {{- toYaml .Values.connections.hostAliases | nindent 8 }}
      {{- end }}
      {{- with .Values.initContainers.custom }}
      initContainers:
      {{- toYaml . | nindent 6 }}
      {{- end }}
      containers:
      - name: newai
        image: {{ include "docspace.imageRegistry" (list . .Values.newai.image.registry) }}{{ include "docspace.imageRepository" (list . .Values.newai.image.repository) }}:{{ default .Values.images.tag .Values.newai.image.tag }}
        imagePullPolicy: {{ .Values.newai.image.pullPolicy }}
        {{- if or .Values.containerSecurityContext.enabled .Values.newai.containerSecurityContext.enabled }}
        {{- $csc := dict }}
        {{- if .Values.newai.containerSecurityContext.enabled }}
        {{- $csc = .Values.newai.containerSecurityContext }}
        {{- else }}
        {{- $csc = .Values.containerSecurityContext }}
        {{- end }}
        securityContext: {{- include "docspace.securityContext" $csc | nindent 10 }}
        {{- end }}
        {{- if .Values.newai.lifecycleHooks }}
        lifecycle: {{- toYaml .Values.newai.lifecycleHooks | nindent 10 }}
        {{- end }}
        ports:
        - containerPort: {{ .Values.newai.containerPorts.app }}
          name: app
        {{- if .Values.newai.startupProbe.enabled }}
        startupProbe: {{- omit .Values.newai.startupProbe "enabled" | toYaml | nindent 10 }}
        {{- end }}
        {{- if .Values.newai.readinessProbe.enabled }}
        readinessProbe: {{- omit .Values.newai.readinessProbe "enabled" | toYaml | nindent 10 }}
        {{- end }}
        {{- if .Values.newai.livenessProbe.enabled }}
        livenessProbe: {{- omit .Values.newai.livenessProbe "enabled" | toYaml | nindent 10 }}
        {{- end }}
        resources: {{- toYaml .Values.newai.resources | nindent 10 }}
        env:
        - name: MYSQL_USER
          value: {{ default .Values.connections.mysqlUser .Values.newai.mysqlUser }}
        - name: MYSQL_PASSWORD
          valueFrom:
            secretKeyRef:
              name: {{ template "docspace.mysql.secretName" . }}
              key: {{ .Values.connections.mysqlSecretKeyPassword }}
        - name: REDIS_PASSWORD
          valueFrom:
            secretKeyRef:
              name: {{ template "docspace.redis.secretName" . }}
              key: {{ .Values.connections.redisSecretKeyName }}
        - name: RABBIT_URI
          valueFrom:
            secretKeyRef:
              name: {{ .Release.Name }}-broker-uri
              key: broker_uri
        - name: RABBIT_PASSWORD
          valueFrom:
            secretKeyRef:
              name: {{ template "docspace.broker.secretName" . }}
              key: {{ .Values.connections.brokerSecretKeyName }}
        - name: DOCUMENT_SERVER_JWT_ENABLED
          valueFrom:
            secretKeyRef:
              name: {{ template "docspace.jwt.secretName" . }}
              key: JWT_ENABLED
        - name: DOCUMENT_SERVER_JWT_SECRET
          valueFrom:
            secretKeyRef:
              name: {{ template "docspace.jwt.secretName" . }}
              key: JWT_SECRET
        - name: DOCUMENT_SERVER_JWT_HEADER
          valueFrom:
            secretKeyRef:
              name: {{ template "docspace.jwt.secretName" . }}
              key: JWT_HEADER
        {{- with .Values.newai.extraEnvVars }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
        envFrom:
        - configMapRef:
            name: docspace
        - secretRef:
            name: {{ template "docspace.coreMachineKey.secretName" . }}
        - secretRef:
            name: {{ template "docspace.jwt.secretName" . }}
        volumeMounts:
        {{- if .Values.extraConf.secretName }}
        {{- $context := . }}
        {{- range .Values.extraConf.fileName }}
        - name: custom-file
          mountPath: /app/{{ $context.Values.product.name }}/config/{{ . }}
          subPath: {{ . }}
        {{- end }}
        {{- end }}
        {{- with .Values.newai.extraVolumeMounts }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
        - name: docspace-data
          mountPath: /app/{{ .Values.product.name }}/data
      volumes:
      {{- if .Values.extraConf.secretName }}
      - name: custom-file
        secret:
          secretName: {{ .Values.extraConf.secretName }}
      {{- end }}
      {{- with .Values.newai.extraVolumes }}
      {{- toYaml . | nindent 6 }}
      {{- end }}
      - name: docspace-data
        persistentVolumeClaim:
          claimName: {{ template "docspace.pvc.data.name" . }}
{{- end }}
