{{- if .Values.aiWorker.enabled }}
apiVersion: apps/v1
kind: {{ .Values.aiWorker.kind }}
metadata:
  name: ai-worker
  namespace: {{ include "docspace.namespace" . | quote }}
  labels:
    app: ai-worker
    {{- if .Values.commonLabels }}
    {{- include "docspace.labels.commonLabels" . | trim | nindent 4 }}
    {{- end }}
  {{- if or .Values.commonAnnotations .Values.aiWorker.annotations }}
  {{- $dictValue := default .Values.commonAnnotations .Values.aiWorker.annotations }}
  annotations: {{- include "docspace.annotations" ( dict "keyName" $dictValue "context" $ ) | nindent 4 }}
  {{- end }}
spec:
  {{- if not .Values.aiWorker.autoscaling.enabled }}
  replicas: {{ default .Values.replicas .Values.aiWorker.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      app: ai-worker
      {{- if .Values.commonLabels }}
      {{- include "docspace.labels.commonLabels" . | trim | nindent 6 }}
      {{- end }}
  {{- if eq .Values.aiWorker.kind "StatefulSet" }}
  serviceName: ai-worker
  {{- end }}
  {{- if .Values.aiWorker.updateStrategy }}
  {{- if eq .Values.aiWorker.kind "Deployment" }}
  strategy: {{- include "docspace.update.strategyType" .Values.aiWorker.updateStrategy }}
  {{- else }}
  updateStrategy: {{- include "docspace.update.strategyType" .Values.aiWorker.updateStrategy }}
  {{- end }}
  {{- end }}
  {{- if eq .Values.aiWorker.kind "StatefulSet" }}
  podManagementPolicy: {{ .Values.aiWorker.podManagementPolicy }}
  {{- end }}
  template:
    metadata:
      labels:
        app: ai-worker
        {{- if .Values.commonLabels }}
        {{- include "docspace.labels.commonLabels" . | trim | nindent 8 }}
        {{- end }}
      {{- if or .Values.podAnnotations .Values.aiWorker.podAnnotations }}
      {{- $dictValue := default .Values.podAnnotations .Values.aiWorker.podAnnotations }}
      annotations: {{- include "docspace.annotations" ( dict "keyName" $dictValue "context" $ ) | nindent 8 }}
      {{- end }}
    spec:
      serviceAccountName: {{ include "docspace.serviceAccountName" . }}
      {{- if or .Values.podSecurityContext.enabled .Values.aiWorker.podSecurityContext.enabled }}
      {{- $psc := dict }}
      {{- if .Values.aiWorker.podSecurityContext.enabled }}
      {{- $psc = .Values.aiWorker.podSecurityContext }}
      {{- else }}
      {{- $psc = .Values.podSecurityContext }}
      {{- end }}
      securityContext: {{- include "docspace.securityContext" $psc | nindent 8 }}
      {{- end }}
      affinity:
        podAntiAffinity:
          {{- if eq .Values.podAntiAffinity.type "preferred" }}
          preferredDuringSchedulingIgnoredDuringExecution:
          - podAffinityTerm:
              labelSelector:
                matchExpressions:
                - key: app
                  operator: In
                  values:
                  - ai-worker
              topologyKey: {{ .Values.podAntiAffinity.topologyKey }}
            weight: {{ .Values.podAntiAffinity.weight }}
          {{- else if eq .Values.podAntiAffinity.type "required" }}
          requiredDuringSchedulingIgnoredDuringExecution:
          - labelSelector:
              matchExpressions:
              - key: app
                operator: In
                values:
                - ai-worker
            topologyKey: {{ .Values.podAntiAffinity.topologyKey }}
          {{- end }}
          {{- with .Values.aiWorker.customPodAntiAffinity }}
          {{- toYaml . | nindent 10 }}
          {{- end }}
        {{- with .Values.aiWorker.podAffinity }}
        podAffinity:
          {{- toYaml . | nindent 10 }}
        {{- end }}
        {{- with .Values.aiWorker.nodeAffinity }}
        nodeAffinity:
          {{- toYaml . | nindent 10 }}
        {{- end }}
      {{- if or .Values.nodeSelector .Values.aiWorker.nodeSelector }}
      nodeSelector: {{ toYaml (default .Values.nodeSelector .Values.aiWorker.nodeSelector) | nindent 8 }}
      {{- end }}
      {{- if or .Values.tolerations .Values.aiWorker.tolerations }}
      tolerations: {{ toYaml (default .Values.tolerations .Values.aiWorker.tolerations) | nindent 8 }}
      {{- end }}
      {{- if .Values.imagePullSecrets }}
      imagePullSecrets:
      - name: {{ .Values.imagePullSecrets }}
      {{- end }}
      {{- if .Values.connections.hostAliases }}
      hostAliases: {{- toYaml .Values.connections.hostAliases | nindent 8 }}
      {{- end }}
      {{- with .Values.initContainers.custom }}
      initContainers:
      {{- toYaml . | nindent 6 }}
      {{- end }}
      containers:
      - name: ai-worker
        image: {{ include "docspace.imageRegistry" (list . .Values.aiWorker.image.registry ) }}{{ include "docspace.imageRepository" (list . .Values.aiWorker.image.repository) }}:{{ default .Values.images.tag .Values.aiWorker.image.tag }}
        imagePullPolicy: {{ .Values.aiWorker.image.pullPolicy }}
        {{- if or .Values.containerSecurityContext.enabled .Values.aiWorker.containerSecurityContext.enabled }}
        {{- $csc := dict }}
        {{- if .Values.aiWorker.containerSecurityContext.enabled }}
        {{- $csc = .Values.aiWorker.containerSecurityContext }}
        {{- else }}
        {{- $csc = .Values.containerSecurityContext }}
        {{- end }}
        securityContext: {{- include "docspace.securityContext" $csc | nindent 10 }}
        {{- end }}
        {{- if .Values.aiWorker.lifecycleHooks }}
        lifecycle: {{- toYaml .Values.aiWorker.lifecycleHooks | nindent 10 }}
        {{- end }}
        ports:
        - containerPort: {{ .Values.aiWorker.containerPorts.app }}
          name: app
        {{- if .Values.aiWorker.startupProbe.enabled }}
        startupProbe: {{- omit .Values.aiWorker.startupProbe "enabled" | toYaml | nindent 10 }}
        {{- end }}
        {{- if .Values.aiWorker.readinessProbe.enabled }}
        readinessProbe: {{- omit .Values.aiWorker.readinessProbe "enabled" | toYaml | nindent 10 }}
        {{- end }}
        {{- if .Values.aiWorker.livenessProbe.enabled }}
        livenessProbe: {{- omit .Values.aiWorker.livenessProbe "enabled" | toYaml | nindent 10 }}
        {{- end }}
        resources: {{- toYaml .Values.aiWorker.resources | nindent 10 }}
        env:
        - name: MYSQL_USER
          value: {{ default .Values.connections.mysqlUser .Values.aiWorker.mysqlUser }}
        - name: MYSQL_PASSWORD
          valueFrom:
            secretKeyRef:
              name: {{ template "docspace.mysql.secretName" . }}
              key: {{ .Values.connections.mysqlSecretKeyPassword }}
        - name: REDIS_PASSWORD
          valueFrom:
            secretKeyRef:
              name: {{ template "docspace.redis.secretName" . }}
              key: {{ .Values.connections.redisSecretKeyName }}
        - name: RABBIT_URI
          valueFrom:
            secretKeyRef:
              name: {{ .Release.Name }}-broker-uri
              key: broker_uri
        - name: RABBIT_PASSWORD
          valueFrom:
            secretKeyRef:
              name: {{ template "docspace.broker.secretName" . }}
              key: {{ .Values.connections.brokerSecretKeyName }}
        - name: DOCUMENT_SERVER_JWT_ENABLED
          valueFrom:
            secretKeyRef:
              name: {{ template "docspace.jwt.secretName" . }}
              key: JWT_ENABLED
        - name: DOCUMENT_SERVER_JWT_SECRET
          valueFrom:
            secretKeyRef:
              name: {{ template "docspace.jwt.secretName" . }}
              key: JWT_SECRET
        - name: DOCUMENT_SERVER_JWT_HEADER
          valueFrom:
            secretKeyRef:
              name: {{ template "docspace.jwt.secretName" . }}
              key: JWT_HEADER
        {{- with .Values.aiWorker.extraEnvVars }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
        envFrom:
        - configMapRef:
            name: docspace
        - secretRef:
            name: {{ template "docspace.coreMachineKey.secretName" . }}
        - secretRef:
            name: {{ template "docspace.jwt.secretName" . }}
        volumeMounts:
        {{- if .Values.extraConf.secretName }}
        {{- $context := . }}
        {{- range .Values.extraConf.fileName }}
        - name: custom-file
          mountPath: /app/{{ $context.Values.product.name }}/config/{{ . }}
          subPath: {{ . }}
        {{- end }}
        {{- end }}
        {{- with .Values.aiWorker.extraVolumeMounts }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
        - name: docspace-data
          mountPath: /app/{{ .Values.product.name }}/data
      volumes:
      {{- if .Values.extraConf.secretName }}
      - name: custom-file
        secret:
          secretName: {{ .Values.extraConf.secretName }}
      {{- end }}
      {{- with .Values.aiWorker.extraVolumes }}
      {{- toYaml . | nindent 6 }}
      {{- end }}
      - name: docspace-data
        persistentVolumeClaim:
          claimName: {{ template "docspace.pvc.data.name" . }}
{{- end }}
