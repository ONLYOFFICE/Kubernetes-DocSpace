{{- if .Values.filesWorker.enabled }}
apiVersion: apps/v1
kind: {{ .Values.filesWorker.kind }}
metadata:
  name: files-worker
  namespace: {{ include "apps.namespace" . | quote }}
  labels:
    app: files-worker
    {{- if .Values.commonLabels }}
    {{- include "apps.labels.commonLabels" . | trim | nindent 4 }}
    {{- end }}
  {{- if or .Values.commonAnnotations .Values.filesWorker.annotations }}
  {{- $dictValue := default .Values.commonAnnotations .Values.filesWorker.annotations }}
  annotations: {{- include "apps.annotations" ( dict "keyName" $dictValue "context" $ ) | nindent 4 }}
  {{- end }}
spec:
  {{- if not .Values.filesWorker.autoscaling.enabled }}
  replicas: {{ default .Values.replicas .Values.filesWorker.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      app: files-worker
      {{- if .Values.commonLabels }}
      {{- include "apps.labels.commonLabels" . | trim | nindent 6 }}
      {{- end }}
  {{- if eq .Values.filesWorker.kind "StatefulSet" }}
  serviceName: files-worker
  {{- end }}
  {{- if .Values.filesWorker.updateStrategy }}
  {{- if eq .Values.filesWorker.kind "Deployment" }}
  strategy: {{- include "apps.update.strategyType" .Values.filesWorker.updateStrategy }}
  {{- else }}
  updateStrategy: {{- include "apps.update.strategyType" .Values.filesWorker.updateStrategy }}
  {{- end }}
  {{- end }}
  {{- if eq .Values.filesWorker.kind "StatefulSet" }}
  podManagementPolicy: {{ .Values.filesWorker.podManagementPolicy }}
  {{- end }}
  template:
    metadata:
      labels:
        app: files-worker
        {{- if .Values.commonLabels }}
        {{- include "apps.labels.commonLabels" . | trim | nindent 8 }}
        {{- end }}
      {{- if or .Values.podAnnotations .Values.filesWorker.podAnnotations }}
      {{- $dictValue := default .Values.podAnnotations .Values.filesWorker.podAnnotations }}
      annotations: {{- include "apps.annotations" ( dict "keyName" $dictValue "context" $ ) | nindent 8 }}
      {{- end }}
    spec:
      serviceAccountName: {{ include "apps.serviceAccountName" . }}
      {{- if or .Values.podSecurityContext.enabled .Values.filesWorker.podSecurityContext.enabled }}
      {{- $psc := dict }}
      {{- if .Values.filesWorker.podSecurityContext.enabled }}
      {{- $psc = .Values.filesWorker.podSecurityContext }}
      {{- else }}
      {{- $psc = .Values.podSecurityContext }}
      {{- end }}
      securityContext: {{- include "apps.securityContext" $psc | nindent 8 }}
      {{- end }}
      affinity:
        podAntiAffinity:
          {{- if eq .Values.podAntiAffinity.type "preferred" }}
          preferredDuringSchedulingIgnoredDuringExecution:
          - podAffinityTerm:
              labelSelector:
                matchExpressions:
                - key: app
                  operator: In
                  values:
                  - files-worker
              topologyKey: {{ .Values.podAntiAffinity.topologyKey }}
            weight: {{ .Values.podAntiAffinity.weight }}
          {{- else if eq .Values.podAntiAffinity.type "required" }}
          requiredDuringSchedulingIgnoredDuringExecution:
          - labelSelector:
              matchExpressions:
              - key: app
                operator: In
                values:
                - files-worker
            topologyKey: {{ .Values.podAntiAffinity.topologyKey }}
          {{- end }}
          {{- with .Values.filesWorker.customPodAntiAffinity }}
          {{- toYaml . | nindent 10 }}
          {{- end }}
        {{- with .Values.filesWorker.podAffinity }}
        podAffinity:
          {{- toYaml . | nindent 10 }}
        {{- end }}
        {{- with .Values.filesWorker.nodeAffinity }}
        nodeAffinity:
          {{- toYaml . | nindent 10 }}
        {{- end }}
      {{- if or .Values.nodeSelector .Values.filesWorker.nodeSelector }}
      nodeSelector: {{ toYaml (default .Values.nodeSelector .Values.filesWorker.nodeSelector) | nindent 8 }}
      {{- end }}
      {{- if or .Values.tolerations .Values.filesWorker.tolerations }}
      tolerations: {{ toYaml (default .Values.tolerations .Values.filesWorker.tolerations) | nindent 8 }}
      {{- end }}
      {{- if .Values.imagePullSecrets }}
      imagePullSecrets:
      - name: {{ .Values.imagePullSecrets }}
      {{- end }}
      {{- if .Values.connections.hostAliases }}
      hostAliases: {{- toYaml .Values.connections.hostAliases | nindent 8 }}
      {{- end }}
      {{- with .Values.initContainers.custom }}
      initContainers:
      {{- toYaml . | nindent 6 }}
      {{- end }}
      containers:
      - name: files-worker
        image: {{ include "apps.imageRegistry" (list . .Values.filesWorker.image.registry) }}{{ include "apps.imageRepository" (list . .Values.filesWorker.image.repository) }}:{{ default .Values.images.tag .Values.filesWorker.image.tag }}
        imagePullPolicy: {{ .Values.filesWorker.image.pullPolicy }}
        {{- if or .Values.containerSecurityContext.enabled .Values.filesWorker.containerSecurityContext.enabled }}
        {{- $csc := dict }}
        {{- if .Values.filesWorker.containerSecurityContext.enabled }}
        {{- $csc = .Values.filesWorker.containerSecurityContext }}
        {{- else }}
        {{- $csc = .Values.containerSecurityContext }}
        {{- end }}
        securityContext: {{- include "apps.securityContext" $csc | nindent 10 }}
        {{- end }}
        {{- if .Values.filesWorker.lifecycleHooks }}
        lifecycle: {{- toYaml .Values.filesWorker.lifecycleHooks | nindent 10 }}
        {{- end }}
        ports:
        - containerPort: {{ .Values.filesWorker.containerPorts.app }}
          name: app
        {{- if .Values.filesWorker.startupProbe.enabled }}
        startupProbe: {{- omit .Values.filesWorker.startupProbe "enabled" | toYaml | nindent 10 }}
        {{- end }}
        {{- if .Values.filesWorker.readinessProbe.enabled }}
        readinessProbe: {{- omit .Values.filesWorker.readinessProbe "enabled" | toYaml | nindent 10 }}
        {{- end }}
        {{- if .Values.filesWorker.livenessProbe.enabled }}
        livenessProbe: {{- omit .Values.filesWorker.livenessProbe "enabled" | toYaml | nindent 10 }}
        {{- end }}
        resources: {{- toYaml .Values.filesWorker.resources | nindent 10 }}
        env:
        - name: MYSQL_USER
          value: {{ default .Values.connections.mysqlUser .Values.filesWorker.mysqlUser }}
        - name: MYSQL_PASSWORD
          valueFrom:
            secretKeyRef:
              name: {{ template "apps.mysql.secretName" . }}
              key: {{ .Values.connections.mysqlSecretKeyPassword }}
        - name: REDIS_PASSWORD
          valueFrom:
            secretKeyRef:
              name: {{ template "apps.redis.secretName" . }}
              key: {{ .Values.connections.redisSecretKeyName }}
        - name: RABBIT_URI
          valueFrom:
            secretKeyRef:
              name: {{ .Release.Name }}-broker-uri
              key: broker_uri
        - name: RABBIT_PASSWORD
          valueFrom:
            secretKeyRef:
              name: {{ template "apps.broker.secretName" . }}
              key: {{ .Values.connections.brokerSecretKeyName }}
        - name: DOCUMENT_SERVER_JWT_ENABLED
          valueFrom:
            secretKeyRef:
              name: {{ template "apps.jwt.secretName" . }}
              key: JWT_ENABLED
        - name: DOCUMENT_SERVER_JWT_SECRET
          valueFrom:
            secretKeyRef:
              name: {{ template "apps.jwt.secretName" . }}
              key: JWT_SECRET
        - name: DOCUMENT_SERVER_JWT_HEADER
          valueFrom:
            secretKeyRef:
              name: {{ template "apps.jwt.secretName" . }}
              key: JWT_HEADER
        {{- with .Values.filesWorker.extraEnvVars }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
        envFrom:
        - configMapRef:
            name: docspace
        - secretRef:
            name: {{ template "apps.coreMachineKey.secretName" . }}
        - secretRef:
            name: {{ template "apps.jwt.secretName" . }}
        volumeMounts:
        {{- if .Values.extraConf.secretName }}
        {{- $context := . }}
        {{- range .Values.extraConf.fileName }}
        - name: custom-file
          mountPath: /app/{{ $context.Values.product.name }}/config/{{ . }}
          subPath: {{ . }}
        {{- end }}
        {{- end }}
        {{- with .Values.filesWorker.extraVolumeMounts }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
        - name: docspace-data
          mountPath: /app/{{ .Values.product.name }}/data
      volumes:
      {{- if .Values.extraConf.secretName }}
      - name: custom-file
        secret:
          secretName: {{ .Values.extraConf.secretName }}
      {{- end }}
      {{- with .Values.filesWorker.extraVolumes }}
      {{- toYaml . | nindent 6 }}
      {{- end }}
      - name: docspace-data
        persistentVolumeClaim:
          claimName: {{ template "apps.pvc.data.name" . }}
{{- end }}
