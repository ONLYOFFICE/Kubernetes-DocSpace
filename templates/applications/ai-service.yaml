{{- if .Values.aiService.enabled }}
apiVersion: apps/v1
kind: {{ .Values.aiService.kind }}
metadata:
  name: ai-service
  namespace: {{ include "docspace.namespace" . | quote }}
  labels:
    app: ai-service
    {{- if .Values.commonLabels }}
    {{- include "docspace.labels.commonLabels" . | trim | nindent 4 }}
    {{- end }}
  {{- if or .Values.commonAnnotations .Values.aiService.annotations }}
  {{- $dictValue := default .Values.commonAnnotations .Values.aiService.annotations }}
  annotations: {{- include "docspace.annotations" ( dict "keyName" $dictValue "context" $ ) | nindent 4 }}
  {{- end }}
spec:
  {{- if not .Values.aiService.autoscaling.enabled }}
  replicas: {{ default .Values.replicas .Values.aiService.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      app: ai-service
      {{- if .Values.commonLabels }}
      {{- include "docspace.labels.commonLabels" . | trim | nindent 6 }}
      {{- end }}
  {{- if eq .Values.aiService.kind "StatefulSet" }}
  serviceName: ai-service
  {{- end }}
  {{- if .Values.aiService.updateStrategy }}
  {{- if eq .Values.aiService.kind "Deployment" }}
  strategy: {{- include "docspace.update.strategyType" .Values.aiService.updateStrategy }}
  {{- else }}
  updateStrategy: {{- include "docspace.update.strategyType" .Values.aiService.updateStrategy }}
  {{- end }}
  {{- end }}
  {{- if eq .Values.aiService.kind "StatefulSet" }}
  podManagementPolicy: {{ .Values.aiService.podManagementPolicy }}
  {{- end }}
  template:
    metadata:
      labels:
        app: ai-service
        {{- if .Values.commonLabels }}
        {{- include "docspace.labels.commonLabels" . | trim | nindent 8 }}
        {{- end }}
      {{- if or .Values.podAnnotations .Values.aiService.podAnnotations }}
      {{- $dictValue := default .Values.podAnnotations .Values.aiService.podAnnotations }}
      annotations: {{- include "docspace.annotations" ( dict "keyName" $dictValue "context" $ ) | nindent 8 }}
      {{- end }}
    spec:
      serviceAccountName: {{ include "docspace.serviceAccountName" . }}
      {{- if or .Values.podSecurityContext.enabled .Values.aiService.podSecurityContext.enabled }}
      {{- $psc := dict }}
      {{- if .Values.aiService.podSecurityContext.enabled }}
      {{- $psc = .Values.aiService.podSecurityContext }}
      {{- else }}
      {{- $psc = .Values.podSecurityContext }}
      {{- end }}
      securityContext: {{- include "docspace.securityContext" $psc | nindent 8 }}
      {{- end }}
      affinity:
        podAntiAffinity:
          {{- if eq .Values.podAntiAffinity.type "preferred" }}
          preferredDuringSchedulingIgnoredDuringExecution:
          - podAffinityTerm:
              labelSelector:
                matchExpressions:
                - key: app
                  operator: In
                  values:
                  - ai-service
              topologyKey: {{ .Values.podAntiAffinity.topologyKey }}
            weight: {{ .Values.podAntiAffinity.weight }}
          {{- else if eq .Values.podAntiAffinity.type "required" }}
          requiredDuringSchedulingIgnoredDuringExecution:
          - labelSelector:
              matchExpressions:
              - key: app
                operator: In
                values:
                - ai-service
            topologyKey: {{ .Values.podAntiAffinity.topologyKey }}
          {{- end }}
          {{- with .Values.aiService.customPodAntiAffinity }}
          {{- toYaml . | nindent 10 }}
          {{- end }}
        {{- with .Values.aiService.podAffinity }}
        podAffinity:
          {{- toYaml . | nindent 10 }}
        {{- end }}
        {{- with .Values.aiService.nodeAffinity }}
        nodeAffinity:
          {{- toYaml . | nindent 10 }}
        {{- end }}
      {{- if or .Values.nodeSelector .Values.aiService.nodeSelector }}
      nodeSelector: {{ toYaml (default .Values.nodeSelector .Values.aiService.nodeSelector) | nindent 8 }}
      {{- end }}
      {{- if or .Values.tolerations .Values.aiService.tolerations }}
      tolerations: {{ toYaml (default .Values.tolerations .Values.aiService.tolerations) | nindent 8 }}
      {{- end }}
      {{- if .Values.imagePullSecrets }}
      imagePullSecrets:
      - name: {{ .Values.imagePullSecrets }}
      {{- end }}
      {{- if .Values.connections.hostAliases }}
      hostAliases: {{- toYaml .Values.connections.hostAliases | nindent 8 }}
      {{- end }}
      {{- with .Values.initContainers.custom }}
      initContainers:
      {{- toYaml . | nindent 6 }}
      {{- end }}
      containers:
      - name: ai-service
        image: {{ include "docspace.imageRepository" (list . .Values.aiService.image.repository) }}:{{ default .Values.images.tag .Values.aiService.image.tag }}
        imagePullPolicy: {{ .Values.aiService.image.pullPolicy }}
        {{- if or .Values.containerSecurityContext.enabled .Values.aiService.containerSecurityContext.enabled }}
        {{- $csc := dict }}
        {{- if .Values.aiService.containerSecurityContext.enabled }}
        {{- $csc = .Values.aiService.containerSecurityContext }}
        {{- else }}
        {{- $csc = .Values.containerSecurityContext }}
        {{- end }}
        securityContext: {{- include "docspace.securityContext" $csc | nindent 10 }}
        {{- end }}
        {{- if .Values.aiService.lifecycleHooks }}
        lifecycle: {{- toYaml .Values.aiService.lifecycleHooks | nindent 10 }}
        {{- end }}
        ports:
        - containerPort: {{ .Values.aiService.containerPorts.app }}
          name: app
        {{- if .Values.aiService.startupProbe.enabled }}
        startupProbe: {{- omit .Values.aiService.startupProbe "enabled" | toYaml | nindent 10 }}
        {{- end }}
        {{- if .Values.aiService.readinessProbe.enabled }}
        readinessProbe: {{- omit .Values.aiService.readinessProbe "enabled" | toYaml | nindent 10 }}
        {{- end }}
        {{- if .Values.aiService.livenessProbe.enabled }}
        livenessProbe: {{- omit .Values.aiService.livenessProbe "enabled" | toYaml | nindent 10 }}
        {{- end }}
        resources: {{- toYaml .Values.aiService.resources | nindent 10 }}
        env:
        - name: MYSQL_USER
          value: {{ default .Values.connections.mysqlUser .Values.aiService.mysqlUser }}
        - name: MYSQL_PASSWORD
          valueFrom:
            secretKeyRef:
              name: {{ template "docspace.mysql.secretName" . }}
              key: {{ .Values.connections.mysqlSecretKeyPassword }}
        - name: REDIS_PASSWORD
          valueFrom:
            secretKeyRef:
              name: {{ template "docspace.redis.secretName" . }}
              key: {{ .Values.connections.redisSecretKeyName }}
        - name: RABBIT_URI
          valueFrom:
            secretKeyRef:
              name: {{ .Release.Name }}-broker-uri
              key: broker_uri
        - name: DOCUMENT_SERVER_JWT_ENABLED
          valueFrom:
            secretKeyRef:
              name: {{ template "docspace.jwt.secretName" . }}
              key: JWT_ENABLED
        - name: DOCUMENT_SERVER_JWT_SECRET
          valueFrom:
            secretKeyRef:
              name: {{ template "docspace.jwt.secretName" . }}
              key: JWT_SECRET
        - name: DOCUMENT_SERVER_JWT_HEADER
          valueFrom:
            secretKeyRef:
              name: {{ template "docspace.jwt.secretName" . }}
              key: JWT_HEADER
        {{- with .Values.aiService.extraEnvVars }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
        envFrom:
        - configMapRef:
            name: docspace
        - secretRef:
            name: {{ template "docspace.coreMachineKey.secretName" . }}
        - secretRef:
            name: {{ template "docspace.jwt.secretName" . }}
        volumeMounts:
        {{- if .Values.extraConf.secretName }}
        {{- $context := . }}
        {{- range .Values.extraConf.fileName }}
        - name: custom-file
          mountPath: /app/{{ $context.Values.product.name }}/config/{{ . }}
          subPath: {{ . }}
        {{- end }}
        {{- end }}
        {{- with .Values.aiService.extraVolumeMounts }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
        - name: docspace-data
          mountPath: /app/{{ .Values.product.name }}/data
      volumes:
      {{- if .Values.extraConf.secretName }}
      - name: custom-file
        secret:
          secretName: {{ .Values.extraConf.secretName }}
      {{- end }}
      {{- with .Values.aiService.extraVolumes }}
      {{- toYaml . | nindent 6 }}
      {{- end }}
      - name: docspace-data
        persistentVolumeClaim:
          claimName: {{ template "docspace.pvc.data.name" . }}
{{- end }}
