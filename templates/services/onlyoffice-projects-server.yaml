apiVersion: v1
kind: Service
metadata:
  name: onlyoffice-projects-server
spec:
  ports:
  - port: 5050
    name: http
    targetPort: {{ .Values.ports.appserver.http }}
  selector:
    app: onlyoffice-projects-server
  type: ClusterIP
