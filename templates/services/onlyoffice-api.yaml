apiVersion: v1
kind: Service
metadata:
  name: onlyoffice-api
  labels:
    app: onlyoffice-api
spec:
  ports:
  - port: 5050
    name: http
    targetPort: {{ .Values.ports.appserver.http }}
  selector:
    statefulset.kubernetes.io/pod-name: onlyoffice-api-0
  type: ClusterIP
