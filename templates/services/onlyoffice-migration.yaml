{{- if eq .Values.product.name "docspace" }}
apiVersion: v1
kind: Service
metadata:
  name: onlyoffice-migration
  labels:
    app: onlyoffice-migration
spec:
  ports:
  - port: {{ .Values.containerPorts.appserver.http }}
    name: http
    targetPort: {{ .Values.containerPorts.appserver.http }}
  selector:
    statefulset.kubernetes.io/pod-name: onlyoffice-migration-0
  type: ClusterIP
{{- end }}
