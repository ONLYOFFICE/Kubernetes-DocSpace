apiVersion: v1
kind: Service
metadata:
  name: onlyoffice-backup
  labels:
    app: onlyoffice-backup
spec:
  ports:
  - port: 5050
    name: http
    targetPort: 5050
  selector:
    statefulset.kubernetes.io/pod-name: onlyoffice-backup-0
  type: ClusterIP
