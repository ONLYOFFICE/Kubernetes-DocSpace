apiVersion: v1
kind: Service
metadata:
  name: onlyoffice-files-services
  labels:
    app: onlyoffice-files-services
spec:
  ports:
  - port: 5050
    name: http
    targetPort: {{ .Values.ports.appserver.http }}
  selector:
    statefulset.kubernetes.io/pod-name: onlyoffice-files-services-0
  type: ClusterIP
