apiVersion: v1
kind: Service
metadata:
  name: onlyoffice-notify
  labels:
    app: onlyoffice-notify
spec:
  ports:
  - port: {{ .Values.containerPorts.appserver.http }}
    name: http
    targetPort: {{ .Values.containerPorts.appserver.http }}
  selector:
    statefulset.kubernetes.io/pod-name: onlyoffice-notify-0
  type: ClusterIP
