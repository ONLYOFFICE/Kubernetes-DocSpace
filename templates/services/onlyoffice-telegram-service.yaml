apiVersion: v1
kind: Service
metadata:
  name: onlyoffice-telegram-service
  labels:
    app: onlyoffice-telegram-service
spec:
  ports:
  - port: {{ .Values.containerPorts.appserver.http }}
    name: http
    targetPort: {{ .Values.containerPorts.appserver.http }}
  selector:
    statefulset.kubernetes.io/pod-name: onlyoffice-telegram-service-0
  type: ClusterIP
