apiVersion: v1
kind: Service
metadata:
  name: onlyoffice-people-server
spec:
  ports:
  - port: 5050
    name: http
    targetPort: {{ .Values.ports.appserver.http }}
  selector:
    app: onlyoffice-people-server
  type: ClusterIP
