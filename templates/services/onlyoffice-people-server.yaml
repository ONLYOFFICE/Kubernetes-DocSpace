apiVersion: v1
kind: Service
metadata:
  name: onlyoffice-people-server
spec:
  ports:
  - port: {{ .Values.containerPorts.app }}
    name: app
    targetPort: {{ .Values.containerPorts.app }}
  selector:
    app: onlyoffice-people-server
  type: ClusterIP
