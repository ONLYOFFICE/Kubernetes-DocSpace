apiVersion: v1
kind: Service
metadata:
  name: onlyoffice-files
spec:
  ports:
  - port: {{ .Values.containerPorts.appserver.http }}
    name: http
    targetPort: {{ .Values.containerPorts.appserver.http }}
  selector:
    app: onlyoffice-files
  type: ClusterIP
