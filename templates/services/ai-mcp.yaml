apiVersion: v1
kind: Service
metadata:
  name: ai-mcp
  namespace: {{ include "apps.namespace" . | quote }}
  labels:
    app: ai-mcp
    {{- if .Values.commonLabels }}
    {{- include "apps.labels.commonLabels" . | trim | nindent 4 }}
    {{- end }}
  {{- if .Values.commonAnnotations }}
  annotations: {{- include "apps.annotations" ( dict "keyName" .Values.commonAnnotations "context" $ ) | nindent 4 }}
  {{- end }}
spec:
  ports:
  - port: {{ .Values.aiMCP.containerPorts.app }}
    name: app
    targetPort: {{ .Values.aiMCP.containerPorts.app }}
  selector:
    app: ai-mcp
    {{- if .Values.commonLabels }}
    {{- include "apps.labels.commonLabels" . | trim | nindent 4 }}
    {{- end }}
  type: ClusterIP
