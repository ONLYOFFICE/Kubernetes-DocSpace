{{- if .Values.onlyofficeCrm.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: onlyoffice-crm
spec:
  ports:
  - port: {{ .Values.containerPorts.app }}
    name: app
    targetPort: {{ .Values.containerPorts.app }}
  selector:
    app: onlyoffice-crm
  type: ClusterIP
{{- end }}
