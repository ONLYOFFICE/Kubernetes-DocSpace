{{- if eq .Values.product.name "docspace" }}
apiVersion: v1
kind: Service
metadata:
  name: onlyoffice-clear-events
  labels:
    app: onlyoffice-clear-events
spec:
  ports:
  - port: {{ .Values.containerPorts.app }}
    name: app
    targetPort: {{ .Values.containerPorts.app }}
  selector:
    statefulset.kubernetes.io/pod-name: onlyoffice-clear-events-0
  type: ClusterIP
{{- end }}
