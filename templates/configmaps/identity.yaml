{{- if .Values.identity.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: identity
  namespace: {{ include "apps.namespace" . | quote }}
  {{- if .Values.commonLabels }}
  labels:
    {{- include "apps.labels.commonLabels" . | trim | nindent 4 }}
  {{- end }}
  {{- if .Values.commonAnnotations }}
  annotations: {{- include "apps.annotations" ( dict "keyName" .Values.commonAnnotations "context" $ ) | nindent 4 }}
  {{- end }}
data:
  SPRING_PROFILES_ACTIVE: {{ .Values.identity.env.springProfilesActive | quote }}
  SPRING_APPLICATION_REGION: {{ .Values.identity.env.region | quote }}
  {{- if .Values.identity.serviceAccount.create }}
  HZ_NETWORK_JOIN_MULTICAST_ENABLED: {{ .Values.identity.env.multicast.enabled | quote }}
  HZ_NETWORK_JOIN_KUBERNETES_ENABLED: {{ .Values.identity.env.kubernetes.enabled | quote }}
  HZ_NETWORK_JOIN_KUBERNETES_SERVICENAME: "identity-cache"
  HZ_NETWORK_JOIN_KUBERNETES_NAMESPACE: {{ include "apps.namespace" . | quote }}
  {{- end }}
{{- end }}
