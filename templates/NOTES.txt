RELEASE VERSION: {{ .Chart.Version }}
APP VERSION: {{ .Chart.AppVersion }}

{{- if .Values.connections.appKnownNetworks }}

You have specified the following addresses of known networks from which forwarded headers will be accepted by ONLYOFFICE Apps services:
    "{{ .Values.connections.appKnownNetworks }}"
  Note: Make sure that you have specified all the networks in which the Proxies that you are using in front of ONLYOFFICE Apps services are located. This is necessary for the application to work correctly.

{{- else }}

WARNING: You haven’t specified the addresses of known networks from which forwarded headers will be accepted by ONLYOFFICE Apps services.. The application may not work correctly.

{{- end }}

{{- if and .Values.docs.enabled (ne .Values.docs.connections.dbName .Values.connections.mysqlDatabase) }}

WARNING: ONLYOFFICE Docs is enabled, but its database name "docs.connections.dbName" ({{ .Values.docs.connections.dbName }}) differs from "connections.mysqlDatabase" ({{ .Values.connections.mysqlDatabase }}). By default ONLYOFFICE Docs uses the same database as ONLYOFFICE Apps. Make sure this is intentional.

{{- end }}

{{- if .Values.healthchecks.enabled }}

You can view the status of application services and dependencies at:
  http(s)://APPS-ADDRESS/healthchecks/hc-ui#/healthchecks

{{- end }}

{{- if .Values.tests.enabled }}

You can test ONLYOFFICE Apps services availability and access to connected dependencies using Helm test:
    $ helm test {{ .Release.Name }} --namespace={{ .Release.Namespace }}
  To view the log of the Pod, run the following command:
    $ kubectl logs -f test-docspace --namespace={{ .Release.Namespace }}
  Note: This testing is for informational purposes only and cannot guarantee 100% availability results

{{- end }}
