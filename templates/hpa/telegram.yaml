{{- if .Values.telegram.autoscaling.enabled }}
apiVersion: {{ .Capabilities.APIVersions.Has "autoscaling/v2" | ternary "autoscaling/v2" "autoscaling/v2beta2" }}
kind: HorizontalPodAutoscaler
metadata:
  name: telegram-hpa
  namespace: {{ include "docspace.namespace" . | quote }}
  {{- if .Values.commonLabels }}
  labels:
  {{- include "docspace.labels.commonLabels" . | trim | nindent 4 }}
  {{- end }}
  {{- if or .Values.commonAnnotations .Values.telegram.autoscaling.annotations }}
  {{- $dictValue := default .Values.commonAnnotations .Values.telegram.autoscaling.annotations }}
  annotations: {{- include "docspace.annotations" ( dict "keyName" $dictValue "context" $ ) | nindent 4 }}
  {{- end }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: telegram
  minReplicas: {{ .Values.telegram.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.telegram.autoscaling.maxReplicas }}
  metrics:
  {{- if .Values.telegram.autoscaling.targetCPU.enabled }}
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: {{ .Values.telegram.autoscaling.targetCPU.utilizationPercentage }}
  {{- end }}
  {{- if .Values.telegram.autoscaling.targetMemory.enabled }}
  - type: Resource
    resource:
      name: memory
      target:
        type: Utilization
        averageUtilization: {{ .Values.telegram.autoscaling.targetMemory.utilizationPercentage }}
  {{- end }}
  {{- with .Values.telegram.autoscaling.customMetricsType }}
  {{- toYaml . | nindent 2 }}
  {{- end }}
  {{- with .Values.telegram.autoscaling.behavior }}
  behavior:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}