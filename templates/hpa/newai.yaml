{{- if .Values.newai.autoscaling.enabled }}
apiVersion: {{ .Capabilities.APIVersions.Has "autoscaling/v2" | ternary "autoscaling/v2" "autoscaling/v2beta2" }}
kind: HorizontalPodAutoscaler
metadata:
  name: newai-hpa
  namespace: {{ include "apps.namespace" . | quote }}
  {{- if .Values.commonLabels }}
  labels:
  {{- include "apps.labels.commonLabels" . | trim | nindent 4 }}
  {{- end }}
  {{- if or .Values.commonAnnotations .Values.newai.autoscaling.annotations }}
  {{- $dictValue := default .Values.commonAnnotations .Values.newai.autoscaling.annotations }}
  annotations: {{- include "apps.annotations" ( dict "keyName" $dictValue "context" $ ) | nindent 4 }}
  {{- end }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: newai
  minReplicas: {{ .Values.newai.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.newai.autoscaling.maxReplicas }}
  metrics:
  {{- if .Values.newai.autoscaling.targetCPU.enabled }}
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: {{ .Values.newai.autoscaling.targetCPU.utilizationPercentage }}
  {{- end }}
  {{- if .Values.newai.autoscaling.targetMemory.enabled }}
  - type: Resource
    resource:
      name: memory
      target:
        type: Utilization
        averageUtilization: {{ .Values.newai.autoscaling.targetMemory.utilizationPercentage }}
  {{- end }}
  {{- with .Values.newai.autoscaling.customMetricsType }}
  {{- toYaml . | nindent 2 }}
  {{- end }}
  {{- with .Values.newai.autoscaling.behavior }}
  behavior:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}
