{{- if .Values.filesServices.autoscaling.enabled }}
apiVersion: {{ .Capabilities.APIVersions.Has "autoscaling/v2" | ternary "autoscaling/v2" "autoscaling/v2beta2" }}
kind: HorizontalPodAutoscaler
metadata:
  name: files-services-hpa
  namespace: {{ include "docspace.namespace" . | quote }}
  {{- if .Values.commonLabels }}
  labels:
  {{- include "docspace.labels.commonLabels" . | trim | nindent 4 }}
  {{- end }}
  {{- if or .Values.commonAnnotations .Values.filesServices.autoscaling.annotations }}
  {{- $dictValue := default .Values.commonAnnotations .Values.filesServices.autoscaling.annotations }}
  annotations: {{- include "docspace.annotations" ( dict "keyName" $dictValue "context" $ ) | nindent 4 }}
  {{- end }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: files-services
  minReplicas: {{ .Values.filesServices.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.filesServices.autoscaling.maxReplicas }}
  metrics:
  {{- if .Values.filesServices.autoscaling.targetCPU.enabled }}
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: {{ .Values.filesServices.autoscaling.targetCPU.utilizationPercentage }}
  {{- end }}
  {{- if .Values.filesServices.autoscaling.targetMemory.enabled }}
  - type: Resource
    resource:
      name: memory
      target:
        type: Utilization
        averageUtilization: {{ .Values.filesServices.autoscaling.targetMemory.utilizationPercentage }}
  {{- end }}
  {{- with .Values.filesServices.autoscaling.customMetricsType }}
  {{- toYaml . | nindent 2 }}
  {{- end }}
  {{- with .Values.filesServices.autoscaling.behavior }}
  behavior:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}