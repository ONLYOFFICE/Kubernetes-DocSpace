{{- if .Values.filesWorker.autoscaling.enabled }}
apiVersion: {{ .Capabilities.APIVersions.Has "autoscaling/v2" | ternary "autoscaling/v2" "autoscaling/v2beta2" }}
kind: HorizontalPodAutoscaler
metadata:
  name: files-worker-hpa
  namespace: {{ include "docspace.namespace" . | quote }}
  {{- if .Values.commonLabels }}
  labels:
  {{- include "docspace.labels.commonLabels" . | trim | nindent 4 }}
  {{- end }}
  {{- if or .Values.commonAnnotations .Values.filesWorker.autoscaling.annotations }}
  {{- $dictValue := default .Values.commonAnnotations .Values.filesWorker.autoscaling.annotations }}
  annotations: {{- include "docspace.annotations" ( dict "keyName" $dictValue "context" $ ) | nindent 4 }}
  {{- end }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: files-worker
  minReplicas: {{ .Values.filesWorker.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.filesWorker.autoscaling.maxReplicas }}
  metrics:
  {{- if .Values.filesWorker.autoscaling.targetCPU.enabled }}
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: {{ .Values.filesWorker.autoscaling.targetCPU.utilizationPercentage }}
  {{- end }}
  {{- if .Values.filesWorker.autoscaling.targetMemory.enabled }}
  - type: Resource
    resource:
      name: memory
      target:
        type: Utilization
        averageUtilization: {{ .Values.filesWorker.autoscaling.targetMemory.utilizationPercentage }}
  {{- end }}
  {{- with .Values.filesWorker.autoscaling.customMetricsType }}
  {{- toYaml . | nindent 2 }}
  {{- end }}
  {{- with .Values.filesWorker.autoscaling.behavior }}
  behavior:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}