{{- if .Values.aiWorker.autoscaling.enabled }}
apiVersion: {{ .Capabilities.APIVersions.Has "autoscaling/v2" | ternary "autoscaling/v2" "autoscaling/v2beta2" }}
kind: HorizontalPodAutoscaler
metadata:
  name: ai-worker-hpa
  namespace: {{ include "docspace.namespace" . | quote }}
  {{- if .Values.commonLabels }}
  labels:
  {{- include "docspace.labels.commonLabels" . | trim | nindent 4 }}
  {{- end }}
  {{- if or .Values.commonAnnotations .Values.aiWorker.autoscaling.annotations }}
  {{- $dictValue := default .Values.commonAnnotations .Values.aiWorker.autoscaling.annotations }}
  annotations: {{- include "docspace.annotations" ( dict "keyName" $dictValue "context" $ ) | nindent 4 }}
  {{- end }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: ai-worker
  minReplicas: {{ .Values.aiWorker.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.aiWorker.autoscaling.maxReplicas }}
  metrics:
  {{- if .Values.aiWorker.autoscaling.targetCPU.enabled }}
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: {{ .Values.aiWorker.autoscaling.targetCPU.utilizationPercentage }}
  {{- end }}
  {{- if .Values.aiWorker.autoscaling.targetMemory.enabled }}
  - type: Resource
    resource:
      name: memory
      target:
        type: Utilization
        averageUtilization: {{ .Values.aiWorker.autoscaling.targetMemory.utilizationPercentage }}
  {{- end }}
  {{- with .Values.aiWorker.autoscaling.customMetricsType }}
  {{- toYaml . | nindent 2 }}
  {{- end }}
  {{- with .Values.aiWorker.autoscaling.behavior }}
  behavior:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}