{{- if .Values.ai.autoscaling.enabled }}
apiVersion: {{ .Capabilities.APIVersions.Has "autoscaling/v2" | ternary "autoscaling/v2" "autoscaling/v2beta2" }}
kind: HorizontalPodAutoscaler
metadata:
  name: ai-hpa
  namespace: {{ include "docspace.namespace" . | quote }}
  {{- if .Values.commonLabels }}
  labels:
  {{- include "docspace.labels.commonLabels" . | trim | nindent 4 }}
  {{- end }}
  {{- if or .Values.commonAnnotations .Values.ai.autoscaling.annotations }}
  {{- $dictValue := default .Values.commonAnnotations .Values.ai.autoscaling.annotations }}
  annotations: {{- include "docspace.annotations" ( dict "k  eyName" $dictValue "context" $ ) | nindent 4 }}
  {{- end }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: ai
  minReplicas: {{ .Values.ai.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.ai.autoscaling.maxReplicas }}
  metrics:
  {{- if .Values.ai.autoscaling.targetCPU.enabled }}
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: {{ .Values.ai.autoscaling.targetCPU.utilizationPercentage }}
  {{- end }}
  {{- if .Values.ai.autoscaling.targetMemory.enabled }}
  - type: Resource
    resource:
      name: memory
      target:
        type: Utilization
        averageUtilization: {{ .Values.ai.autoscaling.targetMemory.utilizationPercentage }}
  {{- end }}
  {{- with .Values.ai.autoscaling.customMetricsType }}
  {{- toYaml . | nindent 2 }}
  {{- end }}
  {{- with .Values.ai.autoscaling.behavior }}
  behavior:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}