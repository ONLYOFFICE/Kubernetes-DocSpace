{{- if .Values.healthchecks.autoscaling.enabled }}
apiVersion: {{ .Capabilities.APIVersions.Has "autoscaling/v2" | ternary "autoscaling/v2" "autoscaling/v2beta2" }}
kind: HorizontalPodAutoscaler
metadata:
  name: healthchecks-hpa
  namespace: {{ include "apps.namespace" . | quote }}
  {{- if .Values.commonLabels }}
  labels:
  {{- include "apps.labels.commonLabels" . | trim | nindent 4 }}
  {{- end }}
  {{- if or .Values.commonAnnotations .Values.healthchecks.autoscaling.annotations }}
  {{- $dictValue := default .Values.commonAnnotations .Values.healthchecks.autoscaling.annotations }}
  annotations: {{- include "apps.annotations" ( dict "keyName" $dictValue "context" $ ) | nindent 4 }}
  {{- end }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: healthchecks
  minReplicas: {{ .Values.healthchecks.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.healthchecks.autoscaling.maxReplicas }}
  metrics:
  {{- if .Values.healthchecks.autoscaling.targetCPU.enabled }}
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: {{ .Values.healthchecks.autoscaling.targetCPU.utilizationPercentage }}
  {{- end }}
  {{- if .Values.healthchecks.autoscaling.targetMemory.enabled }}
  - type: Resource
    resource:
      name: memory
      target:
        type: Utilization
        averageUtilization: {{ .Values.healthchecks.autoscaling.targetMemory.utilizationPercentage }}
  {{- end }}
  {{- with .Values.healthchecks.autoscaling.customMetricsType }}
  {{- toYaml . | nindent 2 }}
  {{- end }}
  {{- with .Values.healthchecks.autoscaling.behavior }}
  behavior:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}