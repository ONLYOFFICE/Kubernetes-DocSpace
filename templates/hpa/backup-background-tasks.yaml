{{- if .Values.backupBackgroundTasks.autoscaling.enabled }}
apiVersion: {{ .Capabilities.APIVersions.Has "autoscaling/v2" | ternary "autoscaling/v2" "autoscaling/v2beta2" }}
kind: HorizontalPodAutoscaler
metadata:
  name: backup-background-tasks-hpa
  namespace: {{ include "docspace.namespace" . | quote }}
  {{- if .Values.commonLabels }}
  labels:
  {{- include "docspace.labels.commonLabels" . | trim | nindent 4 }}
  {{- end }}
  {{- if or .Values.commonAnnotations .Values.backupBackgroundTasks.autoscaling.annotations }}
  {{- $dictValue := default .Values.commonAnnotations .Values.backupBackgroundTasks.autoscaling.annotations }}
  annotations: {{- include "docspace.annotations" ( dict "keyName" $dictValue "context" $ ) | nindent 4 }}
  {{- end }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: backup-background-tasks
  minReplicas: {{ .Values.backupBackgroundTasks.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.backupBackgroundTasks.autoscaling.maxReplicas }}
  metrics:
  {{- if .Values.backupBackgroundTasks.autoscaling.targetCPU.enabled }}
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: {{ .Values.backupBackgroundTasks.autoscaling.targetCPU.utilizationPercentage }}
  {{- end }}
  {{- if .Values.backupBackgroundTasks.autoscaling.targetMemory.enabled }}
  - type: Resource
    resource:
      name: memory
      target:
        type: Utilization
        averageUtilization: {{ .Values.backupBackgroundTasks.autoscaling.targetMemory.utilizationPercentage }}
  {{- end }}
  {{- with .Values.backupBackgroundTasks.autoscaling.customMetricsType }}
  {{- toYaml . | nindent 2 }}
  {{- end }}
  {{- with .Values.backupBackgroundTasks.autoscaling.behavior }}
  behavior:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}