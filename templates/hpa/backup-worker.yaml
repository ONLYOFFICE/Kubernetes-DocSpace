{{- if .Values.backupWorker.autoscaling.enabled }}
apiVersion: {{ .Capabilities.APIVersions.Has "autoscaling/v2" | ternary "autoscaling/v2" "autoscaling/v2beta2" }}
kind: HorizontalPodAutoscaler
metadata:
  name: backup-worker-hpa
  namespace: {{ include "docspace.namespace" . | quote }}
  {{- if .Values.commonLabels }}
  labels:
  {{- include "docspace.labels.commonLabels" . | trim | nindent 4 }}
  {{- end }}
  {{- if or .Values.commonAnnotations .Values.backupWorker.autoscaling.annotations }}
  {{- $dictValue := default .Values.commonAnnotations .Values.backupWorker.autoscaling.annotations }}
  annotations: {{- include "docspace.annotations" ( dict "keyName" $dictValue "context" $ ) | nindent 4 }}
  {{- end }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: backup-worker
  minReplicas: {{ .Values.backupWorker.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.backupWorker.autoscaling.maxReplicas }}
  metrics:
  {{- if .Values.backupWorker.autoscaling.targetCPU.enabled }}
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: {{ .Values.backupWorker.autoscaling.targetCPU.utilizationPercentage }}
  {{- end }}
  {{- if .Values.backupWorker.autoscaling.targetMemory.enabled }}
  - type: Resource
    resource:
      name: memory
      target:
        type: Utilization
        averageUtilization: {{ .Values.backupWorker.autoscaling.targetMemory.utilizationPercentage }}
  {{- end }}
  {{- with .Values.backupWorker.autoscaling.customMetricsType }}
  {{- toYaml . | nindent 2 }}
  {{- end }}
  {{- with .Values.backupWorker.autoscaling.behavior }}
  behavior:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}