{{- if .Values.peopleServer.autoscaling.enabled }}
apiVersion: {{ .Capabilities.APIVersions.Has "autoscaling/v2" | ternary "autoscaling/v2" "autoscaling/v2beta2" }}
kind: HorizontalPodAutoscaler
metadata:
  name: people-server-hpa
  namespace: {{ include "docspace.namespace" . | quote }}
  {{- if .Values.commonLabels }}
  labels:
  {{- include "docspace.labels.commonLabels" . | trim | nindent 4 }}
  {{- end }}
  {{- if or .Values.commonAnnotations .Values.peopleServer.autoscaling.annotations }}
  {{- $dictValue := default .Values.commonAnnotations .Values.peopleServer.autoscaling.annotations }}
  annotations: {{- include "docspace.annotations" ( dict "keyName" $dictValue "context" $ ) | nindent 4 }}
  {{- end }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: people-server
  minReplicas: {{ .Values.peopleServer.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.peopleServer.autoscaling.maxReplicas }}
  metrics:
  {{- if .Values.peopleServer.autoscaling.targetCPU.enabled }}
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: {{ .Values.peopleServer.autoscaling.targetCPU.utilizationPercentage }}
  {{- end }}
  {{- if .Values.peopleServer.autoscaling.targetMemory.enabled }}
  - type: Resource
    resource:
      name: memory
      target:
        type: Utilization
        averageUtilization: {{ .Values.peopleServer.autoscaling.targetMemory.utilizationPercentage }}
  {{- end }}
  {{- with .Values.peopleServer.autoscaling.customMetricsType }}
  {{- toYaml . | nindent 2 }}
  {{- end }}
  {{- with .Values.peopleServer.autoscaling.behavior }}
  behavior:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}